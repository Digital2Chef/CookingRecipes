<UserControl x:Class="CookingRecipes.View.UpdatePassPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookingRecipes.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5" Background="GhostWhite" VerticalAlignment="Center" HorizontalAlignment="Center">
        <!--Window for password changes-->
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>

        </Grid.RowDefinitions>
        
        <!--Window's title-->
        <TextBlock Grid.Row="0" Text="{Binding CurrentUser.Username, StringFormat='Change password for user:{0}'}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="Orange"/>

        <!--Old pass-->
        <StackPanel Margin="5" Orientation="Horizontal" Grid.Row="1">
            <Label Content="Old password:" Width="120" Height="35" VerticalAlignment="Center" FontSize="17" Foreground="orange"/>
            <PasswordBox PasswordChanged="oldPass_PasswordChanged" x:Name="oldPass" Width="120" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>
        
        <!--New password-->

        <StackPanel Margin="5" Orientation="Horizontal" Grid.Row="2">
            <Label Content="Password:" Width="120" Height="35" VerticalAlignment="Center" FontSize="17" Foreground="orange"/>
            <PasswordBox PasswordChanged="newPass_PasswordChanged" x:Name="newPass"  Width="120" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>
        
        <!--Confirm password-->
        <StackPanel Margin="5" Orientation="Horizontal" Grid.Row="3">
            <Label Content="Confirm pass:" Width="120" Height="35" VerticalAlignment="Center" FontSize="17" Foreground="orange"/>
            <PasswordBox PasswordChanged="confirm_pass_PasswordChanged"  x:Name="confirm_pass" Width="120" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>
        
        <!--Buttons update && back-->
        <Grid Margin="5" Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>
            
            <!--Update password btn-->
            <Button Content="Update" Margin="5"  Background="Green" Command="{Binding updatePass}" Width="120" Height="40"/>
            
            <!--Back btn-->
            <Button Content="Back" Margin="5" Grid.Column="1" Background="Red" Command="{Binding back}" Width="120" Height="40"/>
        </Grid>
    </Grid>
</UserControl>
