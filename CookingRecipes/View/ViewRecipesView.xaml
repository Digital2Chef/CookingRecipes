<UserControl x:Class="CookingRecipes.View.ViewRecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookingRecipes.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
            <!--This page is to display recipes-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>
        <!--Title-->
        <TextBlock Text="🍲 My Recipes" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Orange" FontSize="20"/>
        
        <!--DataGrid with all recipes-->
        <DataGrid Margin="5" ItemsSource="{Binding Recipes}" HorizontalAlignment="Center" Grid.Row="1" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" x:Name="recipes" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="Food" Binding="{Binding Food}"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Ingredients Number" Binding="{Binding IngredientsNumber}"/>
                <DataGridTextColumn Header="Ingredients" Binding="{Binding IngredientsText}"/>
                <DataGridTextColumn Header="Cooking Time" Binding="{Binding CookingTime}"/>
                <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}"/>
            </DataGrid.Columns>   
         
        </DataGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <!--Edit button-->
            <Button Command="{Binding   goToEditRecipeWindow}" Content="Edit" Background="Orange" Width="120" Height="35"/>
            
            <!--Delete btn-->
            <Button Command="{Binding deleteRecipe}" Content="Delete" Grid.Column="1" Background="Red" Width="120" Height="35"/>
            
            <!--Export to csv btn-->
            <Button Command="{Binding exportRecipe}" Content="Export" Grid.Column="2" Background="Green" Width="120" Height="35"/>
            
            <!--Save recipe in a txt!-->
            <Button Command="{Binding storeRecipeInTxt}" Content="Store recipe" Grid.Column="3" Background="Azure" Width="120" Height="35"/>
            <!--Back btn-->
            <Button Command="{Binding backButton}" Content="Back" Grid.Column="4" Background="Blue" Width="120" Height="35"/>
        </Grid>
    </Grid>
</UserControl>
   