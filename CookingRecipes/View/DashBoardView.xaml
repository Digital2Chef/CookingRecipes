<UserControl x:Class="CookingRecipes.View.DashBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookingRecipes.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  HorizontalAlignment="Center"  Background="GhostWhite" RenderTransformOrigin="0.503,0.501">
        <!-- App's landing page-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            

        </Grid.RowDefinitions>
        
        <!--Title-->
        <TextBlock Grid.Row="0" Text="{Binding CurrentUser.Username, StringFormat='Welcome, {0}'}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="Orange"/>

        <!--Menu buttons-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                

            </Grid.ColumnDefinitions>
            
            
            <!--Open add recipe window-->
            <Button Margin="15" Command="{Binding goToAddRecipeWindow}"  Content="New recipe" Width="120" Height="35" Background="Green" />
            
      
            
            <!--View recipes-->

            <Button Command="{Binding goToViewRecipesWindow}" Margin="15" Grid.Column="1" Width="120" Height="35" Background="Yellow" Content="View recipes" />


        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>   
            
            <!--Add Profile Info-->
            <Button   Margin="15" Grid.Column="0" Command="{Binding profile}"  Width="120" Height="35" Background="Blue" Content="Profile Info" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>

            <!--Logout button-->
        <Button   Margin="15" Grid.Column="1" Command="{Binding logout}"  Width="120" Height="35" Background="Red" Content="Logout" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>
</UserControl>
