<UserControl x:Class="CookingRecipes.View.UserInfoWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookingRecipes.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <Grid Background="GhostWhite" HorizontalAlignment="Center"  VerticalAlignment="Center">
       
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>


        </Grid.RowDefinitions>
           
        
        
            
                <!--Title-->
                <TextBlock Grid.Row="0" Text="{Binding CurrentUser.Username, StringFormat='Personal information for user:{0}'}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="Orange"/>
     
        <ListView Height="Auto" ItemsSource="{Binding UserInfoList}" SelectedItem="{Binding SelectedUser}">

            <ListView.ItemTemplate>
                <DataTemplate>
                <StackPanel  Grid.Row="1">
            <!--List view displaying users information!-->

                            <TextBlock  Text="{Binding Surname, StringFormat=Surname:{0}}" FontSize="14" Margin="0,2"/>

                            <TextBlock Text="{Binding Lastname,StringFormat=Lastname:{0}}" FontSize="14" Margin="0,2"/>

                            <TextBlock Text="{Binding Email,StringFormat=Email:{0}}" FontSize="14" Margin="0,2"/>

                            <TextBlock Text="{Binding Phone,StringFormat=Phone:{0}}" FontSize="14" Margin="0,2"/>


            
            </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            
            </Grid.ColumnDefinitions>
                <!--Buttons--> 
            
                <!--Delete btn-->
            <Button Content="Delete" Grid.Column="0" Width="90" Margin="5" Height="35" Background="Red" Command="{Binding delete}" />
            <!--Back to previews window btn-->
            <Button Grid.Column="1" Content="Back" Width="90" Margin="5" Height="35" Background="Blue"  Command="{Binding back}"/>

              
            </Grid>
        
    </Grid>
    </ScrollViewer>
</UserControl>
