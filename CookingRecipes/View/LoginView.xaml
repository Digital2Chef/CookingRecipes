<UserControl x:Class="CookingRecipes.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookingRecipes.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" Background="GhostWhite">
        <!-- Login Screen-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Title-->
        <TextBlock Text="Login To Recipe Book" Foreground="OrangeRed" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <!--Username -->
        <StackPanel Margin="5"  HorizontalAlignment="Center" Orientation="Horizontal" Grid.Row="1">
            <Label Width="120" Content="Username:" FontSize="20"/>
            <TextBox VerticalContentAlignment="Center" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="120" Height="35"/>
        </StackPanel>

        <!--Password-->
        <StackPanel Margin="5" HorizontalAlignment="Center"  Orientation="Horizontal" Grid.Row="2">
            <Label Width="120" Content="Password:" FontSize="20"/>
            <PasswordBox VerticalContentAlignment="Center" PasswordChanged="password_PasswordChanged"   x:Name="password" Width="120" Height="35"/>
        </StackPanel>

        <!--Login button-->
        <StackPanel Margin="5" HorizontalAlignment="Center"  Orientation="Horizontal" Grid.Row="3">
            <Label Width="120"/>
            <Button Command="{Binding login}" Background="Green" Content="Login" x:Name="login" Width="120" Height="35"/>
        </StackPanel>

        <!--Register btn-->
        <StackPanel Margin="5"  HorizontalAlignment="Center"  Orientation="Horizontal" Grid.Row="4">
            <Label Width="120"/>
            <Button  Content="Register" Background="Orange" Command="{Binding register}" Width="120" Height="35"/>
        </StackPanel>
        
        <!--Close up btn-->
        <StackPanel Margin="5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="5">
            <Label Width="120"/>
            <Button  Content="Exit" Background="Red" Command="{Binding exit}" Width="60" Height="20"/>
        </StackPanel>
    </Grid>
</UserControl>
